import { Component } from '@angular/core';

@Component({
    selector: 'home',
    template: `
    <home class="home">
      <h1>Ok we logged in!</h1>
    </home>
  `
})
export class HomeComponent { }